# VPS Worker Environment Configuration
# Copy this file to .env on your VPS

# Node Environment
NODE_ENV=production

# Redis Queue Configuration (Upstash or your own Redis)
UPSTASH_REDIS_URL=rediss://default:your_password@your-redis-host.upstash.io:6379

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# SMTP Configuration (Zoho or other provider)
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_USER=your_email@yourdomain.com
SMTP_PASS=your_smtp_password

# Worker Configuration
WORKER_CONCURRENCY=5  # Number of concurrent jobs per worker instance

# Optional: If using local Redis instead of Upstash
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
